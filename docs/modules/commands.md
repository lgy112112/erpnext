# Commands 模块（CLI 命令扩展入口）

## 概述

`commands/` 是 ERPNext 的命令扩展入口目录。当前仓库该目录内容很少（主要是 `__init__.py`），但它代表 CLI 扩展的挂载位置。

## 规模（当前仓库）
- 1 个 Python 文件：`commands/__init__.py`

## 何时需要关注

- 新增/修改 Bench 命令或 CLI 管理命令
- 排查某些维护脚本是否通过命令入口暴露

## 对业务流程修改的相关性

- 通常较低
- 但如果你的流程变更需要批量修复/重建/维护命令，可以考虑在此挂载命令入口（视项目规范而定）
